apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pilosa-cluster.fullname" . }}-test-connection"
  labels:
{{ include "pilosa-cluster.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pilosa-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
